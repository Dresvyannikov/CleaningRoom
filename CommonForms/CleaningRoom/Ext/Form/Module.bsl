&НаСервере
Процедура getUpdatesНаСервере() 
	Результат = SDK_Telegram.ПолучитьСпиокСообщений();
	РезультатЗапроса = SDK_Telegram.СформироватьJSON(Результат, Истина);
КонецПроцедуры


&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ПодключитьОбработчикОжидания("ОбработчикФормы",10,Ложь);
КонецПроцедуры

&НаКлиенте
Процедура ОбработчикФормы() 
    getUpdatesНаСервере();
КонецПроцедуры

